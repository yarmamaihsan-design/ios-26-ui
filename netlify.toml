[build]
command = "npm run build"
publish = "dist"  # ← Vite outputs to "dist", CRA uses "build"

# Headers are correct ✅
[[headers]]
for = "/sitemap.xml"
[headers.values]
Content-Type = "application/xml"

[[headers]]
for = "/robots.txt"
[headers.values]
Content-Type = "text/plain"
